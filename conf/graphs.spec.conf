path   = string   # root directory (all other paths are relative from there)
reader = string   # method to use from aras/graphs/loader.py for conversion

triples = string   # file with graph triples

relation labels = None
entity labels   = None
